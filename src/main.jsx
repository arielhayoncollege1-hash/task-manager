import React from 'react'
import ReactDOM from 'react-dom/client'
import TaskManager from '../task-manager-app.tsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TaskManager />
  </React.StrictMode>
)
